# Sprint Plan - 2026-08-24

## Sprint Objective

Create a presentable Neon Cleaner prototype loop this week:

```text
AI opening / bridge clip -> UE Lin Xia rider -> gameplay takeover -> short chase
```

The sprint is successful when the project can show a coherent 30-60 second
prototype where the player takes control of Lin Xia without an identity or
camera shock.

## Current Starting Point

- Branch: `codex/character-continuity-pipeline`.
- UE engine: 5.8 local install.
- UE project: `ue/NeonCleanerUE/NeonCleanerUE.uproject`.
- Startup preview map: `/Game/LinxiaPreview/LVL_Linxia_CharacterPreview`.
- Playable source character: `PlayablePhaseCharacter` using Epic/Fab
  `Paragon: Phase`.
- Last known state: playable preview passed local smoke test and was pushed to
  GitHub as commit `ab8d58f`.

## Weekly Milestones

### Monday: Playable Lin Xia Baseline

Goal: make the current heroine preview stable enough to build on.

Tasks:

- Pull latest branch state and confirm clean repo.
- Open / validate the UE project and startup map.
- Verify `PlayablePhaseCharacter` possession, camera, movement, jump, and run
  animation direction.
- Re-tune hair / lighting only if the current preview still reads incorrectly.
- Capture at least one UE reference image into
  `source/reference/linxia/ue-captures/`.

Exit gate: Gate 1 from `docs/agent-production-workflow.md`.

### Tuesday: Rider And Motorcycle Proxy

Goal: stop showing Lin Xia as a standalone display character and place her in
the intended gameplay identity.

Tasks:

- Build or assemble a black electric motorcycle proxy.
- Place Lin Xia on or beside the proxy with a rear / side-rear readable pose.
- Create a low chase camera that can become the gameplay camera.
- Avoid final-rig complexity; prioritize silhouette and scale.

Exit gate: Gate 2.

### Wednesday: Short Chase Corridor

Goal: create a playable route that proves the game loop.

Tasks:

- Create a 30-60 second elevated road / industrial route.
- Add wet surface, damaged barriers, debris, and readable path direction.
- Add a target convoy or objective ahead.
- Add simple avoidable obstacle beats.
- Connect player start, camera, and control handoff.

Exit gate: Gate 4 draft pass.

### Thursday: AI / UE Handoff Pass

Goal: regenerate or revise the AI bridge content around the UE first frame.

Tasks:

- Capture UE reference pack: rider, motorcycle, chase camera, handoff frame.
- Update AI video prompts so the ending frame matches the UE camera.
- Use occlusion, rain splash, flare, or smoke to hide imperfect continuity.
- Replace bridge media only after the UE first frame is locked.

Exit gate: Gate 3.

### Friday: Demo Packaging And GitHub Handoff

Goal: leave a complete, recoverable prototype.

Tasks:

- Record a demo clip.
- Run a final smoke test.
- Update `docs/handoff.md` with exact run / restore steps.
- Confirm Marketplace assets are excluded and Git LFS is healthy.
- Commit and push the week milestone.

Exit gate: Gate 5.

## First Slice For Today

Slice name: `Playable Lin Xia Baseline`.

Producer tasks:

1. Run repo and UE project checks.
2. Rebuild if needed.
3. Run preview validation scripts.
4. Open the preview map or run a command-line smoke test.
5. Fix blocking character / camera / animation issues.

QA Director checks:

1. Character is Player 0 controlled.
2. `W`, `A`, `S`, `D` directions match camera-relative expectation.
3. Run animation direction matches movement.
4. Character does not read as sliding during simple movement.
5. Hair / face lighting is acceptable for reference capture.
6. No public-repo-only file is required without being documented.

Definition of done:

- The preview can be opened and operated.
- The result is good enough to start the motorcycle proxy slice.
- Any known defects are documented as non-blocking.

## Gate 1 Producer Verification

Status: `CONDITIONAL PASS`.

Completed checks:

- UE module build succeeded after closing the active Live Coding editor session.
- `inspect_phase_character_defaults.py` confirmed the Paragon source mesh
  transform and `PlayablePhaseCharacter` transform match: location
  `(0, 0.000856, -97)`, yaw `270` / `-90`, scale `(1, 1, 1)`.
- `validate_linxia_preview_level.py` passed for
  `/Game/LinxiaPreview/LVL_Linxia_CharacterPreview`.
- Game smoke test passed in normal gameplay mode: Player 0 possessed
  `PlayablePhaseCharacter_1` and moved about 872 units in two seconds.
- Captured current UE reference image:
  `source/reference/linxia/ue-captures/linxia_phase_preview_2026-08-24.png`.

Known issues:

- Local Epic/Fab `PhasePlayerCharacter` logs old UE 5.8 blueprint node warnings
  for `ResetOrientationAndPosition`. The C++ playable path still passes smoke
  test, so this is treated as a local Marketplace compatibility warning unless
  it blocks packaging later.
- The current Phase idle/reference pose is still a low combat stance. Do not use
  it as a front-facing AI identity reference. Use rear / rear-three-quarter
  framing until the rider proxy or final heroine identity pass improves the pose.

QA Director verdict:

- `CONDITIONAL PASS`: the playable baseline is valid for moving into Gate 2
  rider / motorcycle proxy work.
- Condition 1: commit and push the current playable code, scripts, docs, and
  technical preview capture before relying on this state.
- Condition 2: treat `linxia_phase_preview_2026-08-24.png` as a technical
  gameplay proof only, not an AI face or final identity reference.
- Condition 3: Gate 2 must produce a stronger UE reference pack before any AI
  video regeneration resumes.

## Gate 2 Producer Slice - Rider / Motorcycle Proxy

Status: `PRODUCER MINIMAL SLICE READY FOR QA`.

Implemented:

- Added a reproducible UE Python creation script for
  `/Game/LinxiaRiderProxy/LVL_Linxia_RiderProxy`.
- The level keeps a hidden Phase-based Lin Xia playable pawn as the runtime
  Player 0 anchor and uses a visible Phase `PoseableMeshComponent` for the
  review rider silhouette. It does not modify `PlayablePhaseCharacter`.
- Built a programmatic black electric motorcycle proxy from engine basic
  shapes: grounded front / rear wheels, battery spine, seat, handlebar,
  front fork, swingarms, cyan nose light, and restrained magenta side / tail
  accents.
- Added a wet road base, damaged concrete barriers, cold key light, magenta rim
  light, cyan fill light, and three review cameras:
  `Linxia_Rider_HandoffCamera`, `Linxia_Rider_SideCamera`, and
  `Linxia_Rider_RearCamera`.
- Added a validation script that checks required actors, runtime Player 0
  possession, visible Phase mesh continuity, wheelbase, wheel ground contact,
  rider / seat relation, and low handoff camera placement.

Verification:

- `create_linxia_rider_proxy_level.py` ran successfully in Unreal Editor 5.8.
- `validate_linxia_rider_proxy_level.py` passed.
- Logged validation values:
  - visible rider pose root: `(-54, 0, 8)`
  - runtime Player 0 pawn: `(-54, 0, 84)`
  - front wheel: `(146, 0, 43)`
  - rear wheel: `(-146, 0, 43)`
  - wheelbase: `292`
  - seat: `(-58, 0, 104)`
  - handoff camera: `(-430, -250, 118)`, FOV `36`

How to run:

```powershell
& "C:\Program Files\Epic Games\UE_5.8\Engine\Binaries\Win64\UnrealEditor-Cmd.exe" `
  "E:\codex_project\neon-cleaner-demo\ue\NeonCleanerUE\NeonCleanerUE.uproject" `
  -unattended -nop4 -nosplash `
  -ExecutePythonScript="E:\codex_project\neon-cleaner-demo\ue\scripts\create_linxia_rider_proxy_level.py"

& "C:\Program Files\Epic Games\UE_5.8\Engine\Binaries\Win64\UnrealEditor-Cmd.exe" `
  "E:\codex_project\neon-cleaner-demo\ue\NeonCleanerUE\NeonCleanerUE.uproject" `
  -unattended -nop4 -nosplash `
  -ExecutePythonScript="E:\codex_project\neon-cleaner-demo\ue\scripts\validate_linxia_rider_proxy_level.py"
```

Known limitations before Gate 2 QA sign-off:

- This is a static motorcycle proxy, not a final motorcycle rig or vehicle
  control pawn.
- A handoff proof capture exists at
  `source/reference/linxia/ue-captures/linxia_rider_proxy_handoff_2026-08-24.png`.
- The handoff capture script now crops to the UE viewport and toggles Game View,
  removing editor menus, side panels, actor selection, and light icons from the
  proof image.
- Lin Xia's pose is still a rough static riding approximation, not a final
  seated animation or control rig.
- Phase skeleton bone names were inspected and confirmed for the next pose
  refinement pass.
- UE reference screenshots for front / side / rear / rear three-quarter rider
  and motorcycle detail views still need to be captured before AI video work
  resumes.
- QA Director still needs to review the proxy visually against Gate 2. Producer
  status is not a Gate 2 `PASS` yet.

## QA Policy For This Sprint

- No AI video regeneration before the UE reference pack receives QA `PASS`.
- No bridge movie replacement before the AI final frame and UE first frame
  receive QA `PASS`.
- No external demo claim before startup playback, `Take Control`, automatic input
  return, gameplay possession, and GitHub restore notes receive QA `PASS`.
- `CONDITIONAL PASS` is allowed only for prototype polish issues, not for
  control, identity-source, camera-direction, or restore-path issues.
